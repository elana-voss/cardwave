/// Which family of app themes is active. `standard` is the original
/// blue-grey Material look; `neon` is the synthwave reskin (gradient
/// backdrop, pink/cyan accents, brand wordmark). Persisted on `AppSettings`
/// and mirrored on `ThemeNotifier`.
enum ThemeStyleEnum { standard, neon }
