# Build & cache
build/
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
.pub-cache/
.pub/

# Generated symbol / map files
app.*.symbols
app.*.map.json

# slang analysis output (regenerated by `dart run slang analyze`)
lib/i18n/_missing_translations.json
lib/i18n/_unused_translations.json

# Platform
android/.gradle/
android/captures/
android/gradlew
android/gradlew.bat
android/local.properties
android/key.properties
android/app/upload-keystore.jks
android/app/debug-keystore.jks
/android/app/debug
/android/app/profile
/android/app/release

# Misc
*.class
*.pyc
*.swp
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/
**/doc/api/
**/ios/Flutter/.last_build_id
/coverage/

# IDE
.idea/
.vscode/
*.iml
*.ipr
*.iws

# OS
.DS_Store
Thumbs.db

# Logs
*.log

# Secrets (defensive — shouldn't be in app/ anyway)
.env
.test_secrets
test-secrets.env
*.jks
keystore-b64.txt

# Tools (regeneratable binary)
tools/chromedriver/chromedriver.exe

# Name-database build artifacts — reproducible from corpora + scripts.
tools/generate_names/.cache/
tools/generate_names/.progress

# Disposable fresh-install sandbox for the "(Debug & Sandbox)" launch config
# and Windows integration runs (CARDWAVE_APPDATA_DIR / CARDWAVE_LIBRARY_DIR).
.sandbox/

# Local-only work logs / notes — kept out of the repo but trackable on this machine.
docs-local/
