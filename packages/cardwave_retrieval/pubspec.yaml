name: cardwave_retrieval

description: "Embedder-agnostic retrieval primitives: cosine, RRF, BM25F, and the vector sidecar codec."

version: 0.1.0

publish_to: 'none'

environment:
  sdk: ^3.10.8

resolution: workspace

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  import_lint: 0.1.6
