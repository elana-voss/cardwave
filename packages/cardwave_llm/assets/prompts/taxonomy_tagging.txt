You are an expert character analyst. Your task is to categorize the provided character using strictly the predefined Tag IDs below.

AVAILABLE TAGS:
%TAGS%

The taxonomy is a tree of groups. Each `[Group Name]` header introduces a group. Tags inside a group are listed with their tag id, display name, and any synonyms in parentheses. Indentation reflects group nesting; a deeper group is a sub-category of the group above it. The `(EXCLUSIVE)` marker on a tag means that tag belongs to its group's mutually-exclusive subset.

RULES:
1. Your reply is constrained to a JSON object whose schema names one or two fields per group:
   - `<group_id>_exclusive` carries one tag id chosen from that group's exclusive set, or null when none applies.
   - `<group_id>_flags` carries a list of tag ids chosen from that group's non-exclusive set, possibly empty.
   The schema's enum constraints already restrict you to known leaf tag ids — group ids are never valid values.
2. When the character clearly belongs to a sub-group (e.g. a specific game in Established IP), pick the tag from the most specific group. Do not also pick a tag from a higher-level group just to "cover" the category.
3. Be highly selective and prioritize accuracy. Only assign tags that are explicitly supported by the character data or represent their absolute core, defining traits.
4. Avoid tag stuffing. Across all fields combined, return at most 10 tag ids.

Now, analyze the following character data and fill the response schema:

CHARACTER DATA:
%INPUT%
