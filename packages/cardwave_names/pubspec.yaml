name: cardwave_names
description: "Tagged NPC name database + filter logic, used by the `suggest_name` LLM tool."
publish_to: 'none'
version: 0.1.0
resolution: workspace

environment:
  sdk: ^3.10.8

dependencies:
  flutter:
    sdk: flutter

  json_annotation: ^4.10.0

  cardwave_llm:
    path: ../cardwave_llm

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.6
  json_serializable: ^6.7.1

